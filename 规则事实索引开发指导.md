# 规则事实索引开发指导（GLM 执行版）

## 0. 目标结论

本次只做 **规则预计算事实索引第 1 阶段**，目标解决资料问答里这类问题：

- `张小侯是什么系的`
- `莫凡是什么系的`
- `莫凡有哪些法系`
- `谁是风系`

核心思路：

1. 原文资料和 chunks 继续保留，不丢细节。
2. 在上传 / reindex 时，用规则扫描全部 chunks，预计算 `人物 -> 法系` 事实。
3. 问答时优先查事实表；事实命中则用确定性答案，引用指回原文 chunk。
4. 事实表 miss 时再回退现有 RAG。

本阶段 **不做 LLM 全量事实抽取**，不做复杂知识图谱，不做摘要替代原文。

## 1. 非目标

明确不要做：

- 不要引入 LLM fact extraction。
- 不要调用真实模型重建几百万字资料。
- 不要生成独立“总结层”让 LLM 自己写。
- 不要把 facts 当作替代原文的内容源。
- 不要把具体小说角色名、法系结论硬编码进业务逻辑。
- 不要为了此功能重构整套 RAG。
- 不要做联网搜索增强。

本阶段只做可解释、可测试、零 LLM 成本的规则事实索引。

## 2. 为什么要做

现在 query-time RAG 有两个结构性问题：

1. **召回不全**
   小说事实散落在几千章里。问 `莫凡是什么系的` 时，运行时检索只能召回一小批 chunk，可能只看到早期火系、雷系，漏掉后期法系。

2. **误归因**
   chunk 里可能出现：

   ```text
   赵满延的光系魔法保护了张小侯
   ```

   这不能推出张小侯是光系。必须先识别“人物-法系”的绑定关系，而不是看到人物和法系同段出现就归因。

规则事实索引的作用是：在 reindex 阶段扫描全量 chunk，把明确绑定的事实沉淀出来，QA 时直接查事实。

## 3. 数据模型

新增模型：`backend/models/project_knowledge_fact.py`

建议表名：`project_knowledge_facts`

字段：

```python
class ProjectKnowledgeFact(UUIDMixin, TimestampMixin, Base):
    __tablename__ = "project_knowledge_facts"

    project_id: Mapped[str] = mapped_column(GUID(), nullable=False, index=True)
    source_id: Mapped[str] = mapped_column(GUID(), nullable=False, index=True)
    chunk_id: Mapped[str | None] = mapped_column(GUID(), nullable=True, index=True)

    fact_type: Mapped[str] = mapped_column(String(50), nullable=False, index=True)
    subject: Mapped[str] = mapped_column(String(120), nullable=False, index=True)
    predicate: Mapped[str] = mapped_column(String(80), nullable=False, index=True)
    object: Mapped[str] = mapped_column(String(120), nullable=False, index=True)

    confidence: Mapped[str] = mapped_column(String(30), nullable=False, default="explicit", index=True)
    evidence_text: Mapped[str] = mapped_column(Text, nullable=False, default="")
    evidence_start: Mapped[int | None] = mapped_column(Integer, nullable=True)
    evidence_end: Mapped[int | None] = mapped_column(Integer, nullable=True)

    extractor: Mapped[str] = mapped_column(String(80), nullable=False, default="rule.character_system.v1")
    metadata_: Mapped[dict | None] = mapped_column("metadata", JSONValue(), nullable=True)
```

第一阶段只写：

```text
fact_type = "character_system"
predicate = "has_magic_system"
subject = 人物名，如 "张小侯"
object = 法系，如 "风系"
confidence = "explicit" | "weak"
extractor = "rule.character_system.v1"
```

索引建议：

- `(project_id, fact_type, subject)`
- `(project_id, fact_type, object)`
- `(project_id, source_id)`
- `(project_id, chunk_id)`

去重策略：

- 同一个 `project_id + source_id + chunk_id + fact_type + subject + predicate + object + evidence_text` 只保留一条。
- 可以用应用层去重，不强制一开始建复杂唯一索引。

## 4. SQLite 兼容

当前桌面端 SQLite 依赖 `Base.metadata.create_all` 和 `_ensure_incremental_columns`。

必须做：

1. 在 `backend/db/session.py` 的 `init_db` import 列表里加入 `project_knowledge_fact`。
2. 确认新表在已有 SQLite dev 库上启动时可以自动创建。
3. 如果项目有 Alembic migration 习惯，可补 migration；但桌面开发链路至少要保证 `init_db` 能工作。

## 5. 服务模块

新增服务文件：

```text
backend/services/knowledge_fact_index.py
```

建议公共函数：

```python
async def rebuild_source_facts(
    db: AsyncSession,
    project_id: str | UUID,
    source_id: str | UUID,
    *,
    fact_types: list[str] | None = None,
) -> dict:
    ...

async def rebuild_project_facts(
    db: AsyncSession,
    project_id: str | UUID,
    *,
    fact_types: list[str] | None = None,
) -> dict:
    ...

async def query_character_system_facts(
    db: AsyncSession,
    project_id: str | UUID,
    subject: str | None = None,
    system: str | None = None,
    limit: int = 50,
) -> list[ProjectKnowledgeFact]:
    ...
```

新增规则文件（推荐）：

```text
backend/services/knowledge_fact_rules.py
```

把“人物-法系绑定”的可复用规则放这里，避免 `knowledge_source.py` 和 `knowledge_fact_index.py` 互相 import 造成循环。

建议函数：

```python
def extract_character_system_facts_from_text(
    text: str,
    *,
    source_title: str = "",
) -> list[FactCandidate]:
    ...
```

`FactCandidate` 至少包含：

```python
subject: str
object: str
confidence: str
evidence_text: str
evidence_start: int | None
evidence_end: int | None
metadata: dict
```

## 6. 规则抽取要求

第一阶段只抽“明确或较明确的人物-法系绑定”。

### 6.1 正向模式

这些可判定为 `explicit`：

```text
张小侯是风系
张小侯为风系
张小侯觉醒风系
张小侯拥有风系
张小侯修炼风系
张小侯掌握风系
张小侯释放风系魔法
张小侯施展风轨
张小侯的风系星子
张小侯的风系星轨
张小侯的风系魔法
张小侯，你不是风系的吗
```

技能别名可作为辅助线索：

```text
风轨 -> 风系
火滋 / 烈拳 -> 火系
雷印 / 霹雳 -> 雷系
冰蔓 / 冰锁 -> 冰系
地波 / 岩障 -> 土系
遁影 / 影遁 -> 暗影系
```

注意：技能别名第一版只用于 `weak`，除非同句有明显主语和“释放/施展/使用/掌握”等动词。

### 6.2 必须排除的误归因

这些不能抽成目标人物事实：

```text
赵满延的光系魔法保护了张小侯
莫凡释放火系魔法，张小侯在旁观看
张小侯看见唐月老师释放火系
张小侯听说穆宁雪是冰系
张小侯希望自己能觉醒雷系
张小侯被火系魔法攻击
```

判断原则：

- 法系归属必须绑定到 `subject`。
- 如果法系前后绑定的是另一个人物，不要写入当前 subject。
- “看见 / 听说 / 了解 / 知道 / 希望 / 想要 / 被攻击 / 受到”不是拥有关系。

### 6.3 置信度

`explicit`：

- 主语和法系就近绑定，且有“是/觉醒/拥有/修炼/掌握/释放/施展/使用”等绑定词。
- `人物的X系星尘/星轨/魔法/能力`。

`weak`：

- 主语和技能别名就近绑定，但未明确写出法系名。
- 例如 `张小侯使用风轨` 可抽 `风系`，confidence=`weak`。

QA 默认可以展示 `explicit`，`weak` 需要在回答中标注“能力线索”或“疑似/间接证据”。第一版可以先只用 `explicit` 回答。

## 7. 写入时机

### 7.1 单资料 reindex

现有接口：

```text
POST /api/projects/{project_id}/knowledge/sources/{source_id}/reindex
```

当前只做 `chunk_and_save`。改为：

1. `chunk_and_save`
2. 删除该 source 旧 facts
3. 扫该 source 的 chunks
4. 写入新 facts
5. 返回 `fact_count`

返回示例：

```json
{
  "source_id": "...",
  "chunk_count": 7276,
  "fact_count": 126
}
```

### 7.2 全项目 reindex

现有接口：

```text
POST /api/projects/{project_id}/knowledge/reindex
```

改为全项目 chunk 重建后重建 facts，返回：

```json
{
  "source_count": 2,
  "chunk_count": 7278,
  "fact_count": 126
}
```

### 7.3 上传资料

上传完成并 `chunk_and_save` 后，也要调用 `rebuild_source_facts`。

不要让用户必须手动点 reindex 才能用 facts。

## 8. QA 集成

修改位置：

```text
backend/services/knowledge_source.py
ask_knowledge_question
_try_compile_local_qa_answer
```

当 `v2_plan.intent == "character_ability"` 且问题是“某人是什么系 / 有哪些系 / 有哪些能力”时：

1. 先查 `project_knowledge_facts`：
   - `fact_type = "character_system"`
   - `subject = character`
   - `predicate = "has_magic_system"`
   - 默认 `confidence = "explicit"`
2. 如果 facts 命中，直接生成确定性答案。
3. citations 只返回参与回答的 facts 对应 evidence，不要返回无关检索池前几条。
4. facts miss 时，才走现有 `_extract_character_systems` query-time 逻辑。
5. query-time 仍 miss，再走 LLM/RAG fallback。

确定性回答格式：

```text
根据资料索引，**张小侯** 明确绑定的法系有：

- 风系
```

如果同时有 weak：

```text
根据资料索引，**张小侯** 明确绑定的法系有：

- 风系

能力线索（需结合原文确认）：

- 土系：片段中出现“张小侯使用地波”
```

## 9. 反向查询

当用户问：

```text
谁是风系
有哪些人是冰系
```

优先查 facts：

```text
fact_type = "character_system"
object = "风系"
predicate = "has_magic_system"
confidence = "explicit"
```

回答：

```text
根据资料索引，明确绑定 **风系** 的人物有：

- 张小侯
```

facts miss 时回退现有 RAG。

## 10. API

新增两个后端接口即可，前端第一版可以不接 UI，但接口要有。

### 10.1 列出事实

```text
GET /api/projects/{project_id}/knowledge/facts
```

Query 参数：

```text
fact_type?: string
subject?: string
object?: string
source_id?: string
confidence?: string
limit?: int = 100
offset?: int = 0
```

返回：

```json
{
  "items": [
    {
      "id": "...",
      "fact_type": "character_system",
      "subject": "张小侯",
      "predicate": "has_magic_system",
      "object": "风系",
      "confidence": "explicit",
      "source_id": "...",
      "chunk_id": "...",
      "evidence_text": "张小侯，你不是风系的吗...",
      "extractor": "rule.character_system.v1",
      "created_at": "..."
    }
  ],
  "total": 1
}
```

### 10.2 重建事实索引

```text
POST /api/projects/{project_id}/knowledge/facts/rebuild
```

Body：

```json
{
  "source_id": null,
  "fact_types": ["character_system"]
}
```

返回：

```json
{
  "source_count": 2,
  "chunk_count": 7278,
  "fact_count": 126
}
```

## 11. 前端最小改动

第一版不要求做复杂 UI。

最低要求：

1. 资料详情页 / 切片区附近增加一个小按钮：`重建事实索引`。
2. 重建成功 toast 显示 `已重建事实索引：N 条`。
3. 资料问答 citations 如果来自 fact，显示为 `事实证据`，点击仍定位到 source/chunk。

不要求第一版做事实列表管理页。

## 12. 引用要求

这是本次最重要的体验修复之一。

当答案来自 facts：

- citations 只能包含参与答案的 fact evidence。
- 不要把 `ability_table`、`generic_context` 的前几条混进来。
- citation 的 `source_kind` 建议为 `project_knowledge_fact`。
- citation 里带 `source_id` 和 `chunk_id`，前端点击后仍打开原资料 / 原 chunk。

示例 citation：

```json
{
  "source_kind": "project_knowledge_fact",
  "source_id": "...",
  "chunk_id": "...",
  "title": "全职法师笔趣阁txt",
  "snippet": "张小侯，你不是风系的吗...",
  "evidence_type": "character_system_fact",
  "matched_query": "张小侯 -> 风系",
  "score": 1.0
}
```

## 13. 测试要求

必须补测试，不能只测工具函数。

### 13.1 模型 / 写入测试

新增：

```text
test_rebuild_source_facts_extracts_character_system
```

资料：

```text
张小侯，你不是风系的吗，看看能不能把风轨释放出来。
赵满延的光系魔法保护了张小侯。
莫凡释放火系魔法，张小侯在旁观看。
```

断言：

- facts 里有 `张小侯 -> 风系`
- facts 里没有 `张小侯 -> 光系`
- facts 里没有 `张小侯 -> 火系`

### 13.2 API 级问答测试

新增：

```text
test_ask_character_system_uses_fact_index
```

断言：

- 回答包含 `风系`
- 回答不包含 `光系`
- 回答不包含 `火系`
- citations 的 `evidence_type` 包含 `character_system_fact`
- citations 不应出现无关火系技能表

### 13.3 反向查询测试

新增：

```text
test_ask_character_by_system_uses_fact_index
```

问题：

```text
谁是风系？
```

断言：

- 回答包含 `张小侯`
- 不包含 `赵满延`

### 13.4 reindex 测试

新增：

```text
test_reindex_replaces_old_facts
```

流程：

1. 上传资料 A：`张小侯是风系`
2. 确认 facts 有风系
3. 修改资料为：`张小侯是土系`
4. 调 source reindex
5. 确认旧 `风系` fact 被删除，新 `土系` fact 存在

### 13.5 现有回归不能破

必须继续通过：

```bash
cd backend
venv/bin/python -m pytest tests/test_knowledge.py -q
venv/bin/python -m pytest -q

cd ../frontend
npm run build
```

## 14. 性能要求

第一版允许同步执行，但要避免明显卡死。

建议：

- 单 source rebuild 用分页读取 chunks，每批 200 或 500。
- 不要一次性把 7000+ chunks 全载入内存。
- 每个 chunk 的规则扫描必须是纯字符串 / 正则，不调用 LLM。
- facts 写入按批次 `add_all`。

如果一次全项目重建耗时较长，先接受；前端只展示 loading。不要为了第一版引入 Celery/队列。

## 15. 实现顺序

建议拆成 4 个提交：

### Commit 1：模型与服务

- 新增 `ProjectKnowledgeFact` model
- `init_db` 导入新 model
- 新增 `knowledge_fact_rules.py`
- 新增 `knowledge_fact_index.py`
- 单元测试 facts 抽取与去重

### Commit 2：reindex / upload 接入

- source reindex 后重建 source facts
- project reindex 后重建 project facts
- upload 后自动重建 source facts
- 返回 `fact_count`

### Commit 3：QA 优先使用 facts

- `character_ability` 先查 facts
- `character_by_ability` 先查 facts
- facts 命中走确定性回答
- citations 只引用 fact evidence

### Commit 4：API / 前端最小入口 / 回归测试

- `GET /knowledge/facts`
- `POST /knowledge/facts/rebuild`
- 前端按钮与 toast
- API 级回归测试
- 全量测试和 build

## 16. 验收标准

必须满足：

1. 上传 / reindex 后 facts 表有数据。
2. `张小侯是什么系的` 回答 `风系`，不回答 `光系/火系`。
3. `谁是风系` 能从 facts 反查到张小侯。
4. 答案引用只展示真正支撑结论的 fact evidence。
5. 不调用 LLM 也能通过测试。
6. `tests/test_knowledge.py` 通过。
7. 后端全量测试通过。
8. 前端 build 通过。

## 17. 注意事项

- facts 是索引，不是原文。任何答案都必须能追溯 source/chunk。
- 如果规则不确定，宁可不写 fact，也不要写错 fact。
- `weak` 事实不要默认混进“明确绑定”回答。
- 不要把“某人看见别人施法”抽成某人的能力。
- 不要把“元素魔法七系包括...”抽成某个人拥有全部七系。
- 不要把“希望/想要/可能觉醒”抽成事实。
- 不要把 facts 的总结文本写回 `project_sources.summary`。

## 18. 给 GLM 的最终提示

本任务不是“让回答更像 AI”，而是让资料库拥有一个可靠的、可追溯的事实索引。

先把 `人物 -> 法系` 做准、做稳、做可测试。复杂关系、LLM 抽取、漂亮事实卡都放到后续版本。
